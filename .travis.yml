language: node_js
node_js:
  - "node"
services: 
  - postgresql
env:
  - TEST_DIR=backend
before_script:
  - psql -c "CREATE DATABASE npstest;" -U postgres
script: cd $TEST_DIR && npm install && npm test